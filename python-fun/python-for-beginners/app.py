# This is a simple Python script that prints a message and some symbols.
# Python code getrs executed line by line from the top.
# The interpreter reads the code, translates it into machine code, and executes it.

print("i am geri")
print ('o----')
# Everything under '' is a string
# A string is a sequence of characters
print (' ||||') 
# The following line prints 10 stars
# First it evaluates the expression '*' * 10
# Then it prints the result
print('*' * 10)

# identifiert = value. (will be converted to the binary representation of the value)
price = 10 
price = 20
rating = 3.14
# variables are case-sensitive
# True and False are boolean values and are capitalized
is_published = True
print('price')
print(price)

full_name = 'Geri'
age = 26
# Integers are numbers without decimal points
